1> You can use position: relative; to shift an element from its normal position without affecting the layout of other elements
   on the page. This is done by using the top, right, bottom, and left properties to specify how far the element should be moved
   from its original position.